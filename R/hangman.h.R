
# This file is automatically generated, you probably don't want to edit this

#' @rdname jamovi
#' @importFrom jmvcore Options
#' @importFrom R6 R6Class
#' @export
hangmanOptions <- R6::R6Class(
    "hangmanOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            letters = NULL, ...) {

            super$initialize(
                package='jmvgames',
                name='hangman',
                requiresData=TRUE,
                ...)
        
            private$..letters <- jmvcore::OptionVariable$new(
                "letters",
                letters)
        
            self$.addOption(private$..letters)
        }),
    active = list(
        letters = function() private$..letters$value),
    private = list(
        ..letters = NA)
)

#' @import jmvcore
#' @importFrom R6 R6Class
hangmanResults <- R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$..text),
    private = list(
        ..text = NA),
    public=list(
        initialize=function(options) {
            super$initialize(options=options, name="", title="Hangman")
            private$..text <- jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Hangman")
            self$add(private$..text)}))

#' @importFrom jmvcore Analysis
#' @importFrom R6 R6Class
hangmanBase <- R6::R6Class(
    "hangmanBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'jmvgames',
                name = 'hangman',
                version = c(1,0,0),
                options = options,
                results = hangmanResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL)
        }))

#' Hangman
#'
#' 
#' @param data .
#' @param letters .
#' @export
hangman <- function(
    data,
    letters) {

    options <- hangmanOptions$new(
        letters = letters)

    results <- hangmanResults$new(
        options = options)

    analysis <- hangmanClass$new(
        options = options,
        data = data)

    analysis$run()
    analysis$render()

    analysis
}
